# Shared References

This directory stores source references that can be reused across multiple projects.

Rules:

- Do not edit vendor/source references while planning a product.
- Use these references to verify hooks, extension points, data structures, templates, and lifecycle behavior.
- Product-specific files stay in `projects/<project-name>/`.
- Shared platform/plugin references stay in `references/<platform-or-plugin>/`.

